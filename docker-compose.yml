version: '3'

services:
  app:
    build: .
    env_file:
      - ./.env
    volumes:
      - .:/var/myapp
    depends_on:
      - "s3"

  s3:
    image: minio/minio
    env_file:
      - ./.env
    command: server /data
